export interface DownloadItem {
  id: number | string
  filename: string
  url: string
  manifestPath: string
}

export interface DownloadListItem {
  id: number | string
  filename: string
  manifestPath: string
}
